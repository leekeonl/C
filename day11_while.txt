#include <stdio.h>

void main( )
{


	/*
	while 
	  1. C언어의 반복문 중의 하나
	  2. 초기식, 조건식, 증감식 부분이 정형화 되어 있지 않아서 자유로운 형태로 원하는 내용 반복
	  3. 조건식은 생략 할 수 없고, 조건은 반드시 참 또는 거짓으로 결과가 나와야 한다.
	  4. 조건식을 항상 참으로 나타내면 무한반복 한다.

	  ex )		(1)초기식 --------------------> 생략가능
			while( 2 조건식 ) {
				3 명령문 ; -----------------> 반복할 문장
			}

				
	*/

//----------------------------------------------------------------

	// 예제 1 - 문장 출력
	/*
	int  num = 0;
	 
	while ( num < 5 )
	{
		printf("Hello world %d \n", num);
		num++;
	}
	*/

//----------------------------------------------------------------

	// 예제 2 - 입력받은 구구단 출력
	/*
	int  dan=0, num=1;
	printf("몇 단 ?? ");
	scanf("%d", &dan);
	
	while (num < 10)
	{
		printf("%d * %d = %d \n", dan, num, dan*num);
		num++;
	}
	*/

//-----------------------------------------------------------------

	//예제 3 - 123일때 반복문 탈출
	/*
	int  num = 0;
	
	while ( 1 )
	{
		printf("Hello world %d \n", num);
		num++;
		
		if(num == 123)
			break;
	}
	*/

//-----------------------------------------------------------------

	// Q-1 : 입력받은 수만큼 문자 출력
	/*
	int num = 0; // 반복의 끝을 입력 받을 변수
	int i=1 ; // 반복하는 변수
 
	printf("수를 입력하시오 : ");
	scanf("%d", &num);

	while( i <= num ){
		printf(" Hello world : %d \n", i) ;
		i++;
	}
	*/
	
	//Q-2 : 입력받은 수까지의 3의 배수 출력
	/*
	int num = 0 , i=1 ;

	printf("수를 입력하시오 \n") ;
    scanf(" %d" , &num);
	//입력 받은 수까지 반복
    while( i <= num ){
		// i가 3의 배수 일때만 출력
		if(i%3 == 0){
			printf("%d " , i);
		}
		i++;
	}
	*/
	
    //Q-3 : 1부터 입력받은 수까지의 합
	/*
	int num , i=1, sum=0  ;
	
	printf("수를 입력 하시오 : ");
	scanf("%d", &num) ;

	while( i <= num ){
	    sum += i ;	// i의 값을 sum에 누적
	    printf("%d까지의 합은 = %d\n", i, sum);
	    i++;
	}
	printf("1부터 %d까지의 합은 = %d\n", num, sum);
	*/
	


	//Q-4 : 1부터 100사이에 7의 배수와 9의 배수 출력
	/*
	int  i=1 ;
	// 1부터 100 까지 반복
	while( i <= 100 )
	{
		// i가 7의 배수이기도 하고, 9의 배수 일때
		if( i%7 == 0 || i%9 == 0)
			printf("%d\n", i );
		i++;
	}
	*/


	//Q-5 : 입력 받은 수의 구구단 역순
	/*
	int dan = 0 , num = 9 ;
	//표시 할 단 입력
	printf("몇 단 ???");
	scanf("%d", &dan);
	// num이 1보다 크거나 같을때까지 반복
	while(num>=1)
	{
		printf("%d * %d = %d \n" , dan, num , dan*num);
		num--;
	}
	*/
		
//=========================================================================
   
	// 한줄에 별('*') 5개씩 5줄 찍기
	/*
	int i = 0;  // 줄변수
	int j = 0;	// 별을 찍는 변수

	while(i<5){
		j=0;
		while(j<5-i){
			printf("*");
			j++;
		}
		i++;
		printf("\n");
	}
	*/
    
    // Q-2-1 별표 출력
	/*
	int i=0 , j=0 ;
	
	while(i<5){
	     j=1 ;
	     while(j<=5-i){
		  printf("*");
		  j++;
	     }
	     printf("\n");
	     i++;
	}
	*/

//====================================================
       
	//Q-2-2
	/*
    	int i=0 , j=0 ;
	
   	 while( i < 5 ){
	     j=1 ;

	     while( j <= i ){
			printf("o");
			j++;
	     }

	     printf("*\n");
	     i++;
	}
	*/

//===================================================
	
	//Q-2-3
	/*
	int i=0;
	int su, total=0;
	
	 while( i < 5 ){
	printf("%d번째 입력 : ", i);
	scanf("%d", &su);

	if( su >= 1 ){	// 입력이 1 이상일때만 총합에 더하기
			total += su;
			i++;
		}

		printf("su = %d, total = %d, i = %d\n", su, total, i);
	}
	printf("합계 : %d\n", total);
	*/
}